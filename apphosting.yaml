# This file is for server-based deployments on Firebase App Hosting.
# As we are deploying a static site (output: 'export'), this file is not needed.
# Firebase will automatically detect the /out folder and serve it.
